## 2026-07-15 16:58:00 -0400
Fixed the base-image bootstrap failure seen on a fresh remote host. The configured artifact checksum was sound, but the URL pointed at Ubuntu's dated daily-build tree, whose older directories are eventually removed. I changed the default to Ubuntu's retained `release-20260225` archive, which contains the same amd64 image under the release filename and the same pinned SHA256, and added a compatibility rewrite so configs generated with the former built-in URL recover automatically.

The second issue was command-plan sequencing rather than curl itself: the transfer used `check=False`, so the grouped plan continued to `mv` and `sha256sum` before Python inspected the transfer result. I made the transfer a checked command. A failed curl/copy now aborts the plan immediately, before the staging path can be promoted or hashed, and the caller receives a domain-level error explaining that the cache was not installed. I deliberately did not add a moving `current/` URL or online checksum discovery; reproducibility and a small trust surface are more important than silently following the newest image.

The remaining tradeoff is that an interrupted transfer may leave a `.part` file until the next retry. That is safe because it is never promoted, and the first command of every retry removes it. I am confident the original 404 is repaired for both new and existing configs and that future transfer failures cannot be masked by a secondary `mv: cannot stat` error.

## 2026-07-24 14:01:42 -0400
Implemented VM-scoped repository credentials under `aivm vm creds`, using one dedicated GitHub deploy key for each VM/repository pair. The host creates and retains the keypair, registers only the public key through `gh`, installs the private key into the selected guest, and writes exact Git URL rewrites so normal clone/pull/push commands select the correct per-repository identity. The config store now records non-secret credential metadata under the owning VM, and revocation invalidates the provider key before attempting guest or host cleanup.

I deliberately kept branch policy out of AIVM and made read-only the default with an explicit `--write` opt-in. I also chose to block VM deletion while an active credential exists rather than silently revoking or orphaning access during an unrelated lifecycle command. A provider-revoked credential may remain in `revocation-pending` when the VM is unreachable; deletion is then safe and removes the remaining host state. The main remaining risk is integration behavior against a real GitHub repository and real `kwconf` dispatch, because this environment's package mirror repeatedly returned HTTP 503 and direct PyPI DNS was unavailable. The resolver, store schema, generated guest configuration, mocked provider lifecycle, failure recovery, deletion guard, and direct dry-run command were exercised locally; I am confident in the state transitions and conservative revocation ordering, but a live deploy-key smoke test should still be done before release.

## 2026-07-24 14:26:47 -0400
Performed an adversarial review of the initial VM deploy-key implementation after its tests passed in the user's environment. The happy-path lifecycle was coherent, but the review exposed security-sensitive assumptions that the original tests did not challenge. In particular, remote-key selection preferred the mutable host public-key file over the credential identity recorded in the config store. A changed host key could therefore redirect revocation toward a different deploy key. I changed provider identity resolution to use the stored provider id and SHA256 fingerprint, reject mismatches, and verify that GitHub no longer reports the key before local cleanup.

I also made credential parsing fail closed. Previously, a malformed nested credential table could be silently discarded, which weakened the VM-deletion guard and could orphan a live provider authorization. Required fields, supported states/access modes, duplicate ids, and duplicate repository scopes are now validated by both the parser and config lint. Guest SSH configuration now uses batch mode and first-use host-key acceptance so a fresh VM does not hang or fail on the first Git operation. Git URL rewrites were narrowed to canonical `.git` URLs because Git's `insteadOf` matching is prefix-based and the broader rewrite could capture similarly named sibling repositories.

The expanded tests cover split-store round trips, malformed and duplicate records, repository-input injection syntax, host-key fingerprint drift, remote-id/fingerprint disagreement, failed provider deletion verification, read-only provisioning, first-use SSH settings, and Git rewrite prefix collisions. Forty-two targeted tests pass in the local dependency-stub environment; one real `kwconf` dispatch test remains deselected because the package mirror is unavailable here. I am confident the highest-risk local state and revocation failures are now covered. The remaining release risk is a live GitHub/VM end-to-end smoke test, particularly actual deploy-key creation, first SSH connection, pull, push for write grants, and provider revocation.

## 2026-07-24 15:20:00 -0400
Reviewed a second-agent security pass against the deploy-key hardening patch. The review correctly found a `ty` inference failure in config lint, unsafe trust in persisted credential ids and repository fields, a false-success path for checkout remotes lacking `.git`, an unhandled malformed-public-key status path, and silent loss of custom SSH ports. I fixed the tuple typing directly and centralized credential identity validation. Persisted ids must now match `git-[0-9a-f]{12}` and the deterministic VM/repository hash; repository components and metadata reject control characters; host and guest path helpers independently reject invalid ids before constructing deletion targets.

I chose to reject explicit transport URLs without `.git` rather than reintroduce Git's prefix-based `insteadOf` collision. `OWNER/REPO` shorthand remains supported, while a local checkout with a noncanonical remote receives a clear error and must normalize its remote first. Explicit ports are also rejected until AIVM models the provider API endpoint and SSH endpoint separately. Status catches malformed host public keys and reports structured drift instead of aborting. The credentials package now uses lazy exports so config parsing can import validation without creating a config-store/service cycle. Fifty-two targeted credential, store, and config-lint tests pass in the dependency-stub environment, with the real `kwconf` dispatch test still deselected because the package mirror is unavailable.

## 2026-07-24 16:05:00 -0400
Addressed the remaining second-agent re-review findings for VM deploy-key credentials. Explicit transport URLs are now restricted to canonical HTTPS or SSH forms that the generated Git configuration actually rewrites: HTTPS userinfo, HTTP, `git://`, non-`git` SSH users, mixed-case/noncanonical spellings, and explicit ports are rejected. Guest verification first asks Git to expand the selected URL with `git ls-remote --get-url` and refuses network access unless the result is the exact credential-specific `aivm-cred-*` SSH alias. This closes the public-repository false-success path and prevents URL userinfo from reaching command logs.

Host credential inspection now validates the complete keypair. Credential directories and key files must be current-user-owned, non-symlink filesystem objects with safe permissions; `ssh-keygen -y` derives the public key from the private key, and AIVM compares that result with both the stored public file and recorded fingerprint. Existing but untracked key material is no longer adopted. I also added `aivm vm creds abandon ... --provider_unverified` as an explicit recovery path when the provider cannot be inspected. It attempts guest cleanup, removes the host copy, retains a non-secret audit tombstone recording which cleanup steps could not be verified, and never claims provider revocation. Finally, VM deletion now removes `revocation-pending` host key material before destroying the domain and aborts without deleting records on any cleanup error other than an already-missing directory.

## 2026-07-24 20:45:10 -0400
Performed a behavior-preserving organization pass on the VM credential feature after the security review converged. The main goal was to reduce local sprawl without changing the now-reviewed lifecycle ordering. Credential kind, access, and state literals now live in a dependency-light schema module with named predicates for guest usability and VM-deletion eligibility. Config parsing, config lint, lifecycle code, and the typed store model all consume that single source of truth.

I moved host key trust-boundary checks, keypair inspection, and generation from `credentials/service.py` into `credentials/keys.py`, and moved recorded provider-key identity selection into `credentials/github.py`. The two provider lookups now have names that distinguish discovery of a newly added key by public material from recovery lookup using the immutable recorded id/fingerprint. I also extracted the repeated CLI credential-selector logic used by status, revoke, and abandon. `service.py` remains responsible for orchestration and persistence ordering rather than becoming a lifecycle class or provider framework.

The main risk in this pass was accidental behavior drift while moving security-sensitive code. I updated tests and monkeypatch seams to follow the new ownership, added focused tests for the state predicates and shared selector helper, and kept the large test-file split and config-store/domain ownership cleanup out of scope. In the reduced dependency-stub environment, 55 credential tests pass with seven host/environment skips and one real `kwconf` dispatch test deselected; 24 store/lint tests pass, compilation succeeds, and the diff has no whitespace errors. I am confident this improves navigation and reduces duplicated state strings without changing authorization or recovery semantics, but the user's real environment should still run `ty`, Ruff, and the complete test matrix before committing.


## 2026-07-27 10:35:00 -0400
Added a host-focused `aivm vm creds setup` workflow after the first live credential attempt exposed two avoidable onboarding failures: GitHub CLI may not be installed or authenticated, and a shared top-level AIVM data directory may intentionally retain group-write permissions. The new command checks `gh`, `ssh`, and `ssh-keygen`, installs missing packages through apt on Debian/Ubuntu, starts the GitHub browser login without uploading the user's ordinary SSH key, and optionally proves that the active identity can list deploy keys for a selected repository. `setup --check` provides the diagnostic-only form rather than introducing a separate doctor command with overlapping responsibility.

I kept per-credential `status` focused on drift after a record exists. Broad directory *modes* are now a one-time warning with a `chmod 700` remedy at every managed level -- application-data root, VM directory, credential parent, and credential leaf -- because a policy that stops at the root still blocks a workstation whose whole data tree inherited `0775`. `behavior.credential_directory_permission_policy` selects `warn` (default), `error`, or `ignore`; ownership, real-directory and file-type checks, and private/public key validation still fail closed in every mode. This accepts a collaborative home-directory policy while preserving the strongest checks around the actual secret material. The main security tradeoff is explicit: a group-writable parent is a weaker trust boundary, so the warning must remain visible and should not be softened further.

The implementation deliberately leaves the existing GitHub backend intact so it composes with newer deploy-key pagination and identity-hardening work. Focused setup tests live in a separate module rather than replacing the already fast-moving credential regression file.
